export default class BaseOrdenador {

    ordenarCrescente = (a, b) => {
        throw new SyntaxError()
    }
    ordenarDecrescente = (a, b) => {
        throw new SyntaxError()
    }
    getLabelCss = () => {
        throw new SyntaxError()
    }
}